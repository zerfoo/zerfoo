## Last Updated: 2026-04-16 20:55 UTC

## Completed This Session:
- T99 investigation (docs-only): filed three follow-ups for gemma4e
  capture/throughput/correctness issues; PR #488 merged as 16055f29.

## Investigation findings (docs/devlog.md 2026-04-16):
- T99.1.3 attempted on main (6ad8bceb): capture fails at LMHead
  (instruction 568, axes 3 vs dims 2). Fallback runs at 1.17 tok/s.
- Capture-off on main: 1.23 tok/s. Capture-off at 72828131: 2.69 tok/s
  (2.2x regression, not yet bisected).
- 3.85 tok/s figure in docs/benchmarks.md did not reproduce; annotated
  as unverified.
- gemma4e decode produces degenerate output on CPU + GPU at BOTH
  commits measured; "40 bytes non-degenerate output" from the E98
  run was forward-mode, not generate-mode. Decode has never been
  validated for coherence.
- Did NOT revert T99.1.2. T99.1.2 did what ADR-088 Option C specified;
  decode breakage is pre-existing.

## Plan Updates:
- T99.1.3: BLOCKED by T99.1.4 + T99.2.2 (reason captured in plan.md)
- T99.1.4 (NEW): make LMHead CUDA-graph-capture compatible. ~2-4h.
  Unblocker for T99.1.3's capture-completes requirement.
- T99.2.1 (NEW): bisect and fix 2.69 -> 1.23 tok/s GPU throughput
  regression. Prime suspect T99.1.2 pre-slicing. Fix forward; do
  NOT revert.
- T99.2.2 (NEW): gemma4e decode correctness (degenerate output on
  all commits/devices). Epic-level. Blocks any user-facing gemma4
  throughput claims.

## In Progress:
- None

## Next Up:
- T99.1.4 LMHead capture compat -- best scoped next step, ~2-4h,
  no dependencies. Unblocks T99.1.3's graph-capture requirement
  independent of T99.2.1/T99.2.2.
- T99.2.1 throughput regression bisect -- larger, needs DGX bench
  iterations on multiple commits between 72828131 and 6ad8bceb.
- T99.2.2 decode correctness -- highest impact, largest scope;
  may need a cross-device regression test before root-causing.

## Known Blockers:
- gemma4e generate produces degenerate output on all measured
  commits/devices (tracked as T99.2.2). Until fixed, all gemma4e
  throughput numbers are on gibberish.

## Merge Status:
- PR #488 (docs-only T99 investigation): MERGED as 16055f29
- t99.1.3-enable-cuda-graph: abandoned (manifest-only change not
  shipped; capture does not complete, AC not met)

## Ship Status:
- PR: https://github.com/zerfoo/zerfoo/pull/488 -- MERGED
- CI: PASS (Analyze Go, CodeQL, bench, build amd64/arm64, test 1.26)
- Staging: N/A (docs-only)
- Release: N/A (docs-only, no version bump)

## Deviations This Session:
- [Deviation: Architectural] T99.1.3 AC not met on current main;
  the task pivoted from "re-verify capture works" to "file the
  three underlying bugs that block it". User explicitly approved
  the pivot.

## DGX Spark state:
- Binary at /var/lib/zerfoo/bin/gemma4_e2e rebuilt from 6ad8bceb
  (Apr 16 10:21). Baseline .72828131 binary cleaned up. DGX
  ~/zerfoo on main at 6ad8bceb.
