## Last Updated: 2026-03-11 UTC

## Completed This Session:

### Wave 9 (sequential):
- T108.1 completed: Fixed PowScalar SIGSEGV (AAPCS64 float-in-integer-register
  mismatch). Changed elementwise.cu and rmsnorm.cu launchers to accept unsigned
  int, reinterpret via memcpy (commits d787888, e20604c). Fixed gatherOp
  index-out-of-range panic (commit 22d269b). bench_tps runs on DGX Spark:
  10 tokens at 0.44 tok/s with per-op fallback.

### Wave 8 (sequential):
- T108.1 attempted: bench_tps builds without -tags cuda. libkernels.so built
  on DGX Spark (sm_121, commit f9c9b74). Run crashes in PowScalar kernel
  (SIGSEGV addr=0x1680) -- same crash with and without -tags cuda. Pre-existing
  kernel argument mismatch bug, not related to our build tag removal.

### Wave 7 (sequential):
- T107.2 + S107.2.1: go build ./... and core tests pass on DGX Spark.
  Fixed MatMul nil input SIGSEGV (2169520) and inference test (f07754b).

### Wave 6 (sequential):
- T107.1: go build ./... passes on macOS. Fixed model/ zmf issue.

### Waves 1-5 (prior):
- E101-E106 all complete. E104 unified inference engine. E103 BLAS/DNN factory.

## In Progress:
- None

## Next Up:
- T108.2: Compare megakernel vs plan.Run() output.
- T108.3: Run golangci-lint on all modified packages.
- S108.3.1: Update docs and checkpoint.

## ADR-025 Build Tag Removal Status:
- go build ./... passes without -tags cuda on both macOS and DGX Spark.
- All pure Go CUDA files have build tags removed.
- Runtime cuda.Available() detection works correctly.
- Inference engine unified with runtime detection.
- BLAS/DNN optional via factory pattern.
- 16 missing op emitters added.
- libkernels.so Makefile target added.
- PowScalar kernel SIGSEGV fixed (AAPCS64 float register mismatch).
- gatherOp bounds check added (graceful fallback for non-traced Compile).
- bench_tps generates tokens on DGX Spark (0.44 tok/s with per-op fallback).

## Known Blockers:
- TestCPUEngine_Exp pre-existing floating point precision failure.
- internal/cublas/ and internal/cudnn/ remain behind //go:build cuda (Phase 2).
- Megakernel Gather emission fails for non-traced Compile path (falls back to
  per-op correctly, but megakernel is not fully fused).

## Deviations This Session:
- [Bug] MatMul nil input check was after Q4 storage assertion -- fixed (2169520).
- [Bug] inference TestCreateEngine_GPUDevices expected CUDA to always error -- fixed (f07754b).
- [Blocker] tensor stub files conflicted with Wave 3 -- fixed (fff635b).
- [Bug] PowScalar SIGSEGV: AAPCS64 float-in-integer-register mismatch -- fixed (d787888, e20604c).
- [Bug] gatherOp index out of range for single-input Gather -- fixed (22d269b).

## Branch: feat/tensor-runtime-gpu-detection
## Latest commit: 22d269b
## Working directory: /Users/dndungu/Code/zerfoo/zerfoo
