## Last Updated: 2026-03-12 UTC

## Completed This Session:

### Wave 10 (parallel, 2 agents):
- T108.2: Compared CPU vs CUDA output on DGX Spark. Megakernel falls back to
  plan.Run() (Gather 1-input error, expected). CPU: 5.71 tok/s, CUDA: 2.22
  tok/s. Both produce degenerate output. Outputs do NOT match -- pre-existing
  inference correctness issue, not introduced by ADR-025.
- T108.3: go vet clean. 5 pre-existing unsafe.Pointer warnings in purego.
  No new lint warnings from our changes. golangci-lint not available.
- S108.3.1: Plan, checkpoint, and updates documented.

### Wave 9 (sequential):
- T108.1: Fixed PowScalar SIGSEGV (AAPCS64 float-in-integer-register mismatch,
  commits d787888, e20604c). Fixed gatherOp panic (commit 22d269b). bench_tps
  runs on DGX Spark: 10 tokens at 0.44 tok/s with per-op fallback.

### Prior Waves (1-8):
- E101-E107 all complete. E108 T108.1 attempted in Wave 8 (blocked by kernel
  bug), fixed in Wave 9.

## In Progress:
- None

## Next Up:
- All ADR-025 plan tasks (E101-E108) are complete.
- Track B performance tuning tasks (T94.1-T95.4) are pending but outside
  current plan scope (after M64).

## ADR-025 Build Tag Removal -- COMPLETE:
- go build ./... passes without -tags cuda on both macOS and DGX Spark.
- All pure Go CUDA files have build tags removed.
- Runtime cuda.Available() detection works correctly.
- Inference engine unified with runtime detection.
- BLAS/DNN optional via factory pattern.
- 16 missing op emitters added.
- libkernels.so Makefile target added.
- PowScalar kernel SIGSEGV fixed (AAPCS64 float register mismatch).
- gatherOp bounds check added (graceful fallback for non-traced Compile).
- bench_tps generates tokens on DGX Spark.
- Lint clean (no new warnings).

## Known Issues (pre-existing, not our changes):
- CPU and CUDA plan.Run() produce degenerate output (repetitive/nonsensical
  tokens). Both differ from each other. Pre-existing inference correctness issue.
- TestCPUEngine_Exp pre-existing floating point precision failure.
- internal/cublas/ and internal/cudnn/ remain behind //go:build cuda (Phase 2).
- Megakernel Gather emission fails for non-traced Compile path (falls back
  to per-op correctly, but megakernel is not fully fused).
- CUDA per-op path (2.22 tok/s) slower than CPU (5.71 tok/s) on plan.Run().

## Deviations This Session:
- [Bug] PowScalar SIGSEGV: AAPCS64 float-in-integer-register mismatch -- fixed.
- [Bug] gatherOp index out of range for single-input Gather -- fixed.

## Branch: main (PR #52 merged via rebase)
## Latest main commit: 46fe36f
## Working directory: /Users/dndungu/Code/zerfoo/zerfoo
