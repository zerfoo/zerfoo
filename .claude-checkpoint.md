## Last Updated: 2026-03-31 19:00 UTC

## Session Summary: 5 GPU Regression Fixes + Diagnostic Test

### Fixes Shipped (all pushed):
1. ztensor eab19d0: isTransposeReshape square-matrix no-op bug
2. zerfoo 90cacad4: Causal mask D2H bug in SDPA
3. zerfoo 1d56d2e5: Q4_K/Q5_K/Q6_K/Q5_0 lossy re-quantization to Q4_0
4. ztensor e7927e5: Q5_0Storage B-weight handling in CPU MatMul
5. zerfoo a906d3a5: Q5_0Storage virtual transpose in transposeWeight (CPU only; GPU reverted due to ARM64 alignment)

### Key Results:
- GQA parity test (TestGQA_GPUParity): PASS -- maxDiff=2.6e-6, 0 elements >0.01
- Step-by-step parity: ALL steps PASS (qProj, kProj, qNorm, RoPE, attnScores)
- Full inference: CPU "Paris" (correct), GPU still diverges
- The divergence is between GQA-in-isolation (PASSES) and GQA-in-graph (FAILS)

### Root Cause Analysis:
The Q5_0 virtual transpose on GPU causes misaligned address errors (ARM64). The
fallback F32 physical transpose path SHOULD work but the full inference pipeline
still diverges. The graph execution layer (EngineProxy, graph.Forward, constant
tensor management) introduces a difference not present when GQA runs in isolation.

### Next Session:
1. Compare graph-level execution: same GQA weight tensors, but wrap engine in
   EngineProxy and run through graph.Forward instead of direct GQA.Forward.
2. Check if UploadWeights called multiple times (from LoadFile + from graph
   compilation) corrupts tensor storage.
3. Check if the merged QKV optimization (line 503-517 in arch_common.go)
   affects Q5_0 weights differently on GPU.
