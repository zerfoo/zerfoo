## Last Updated: 2026-03-29T00:00:00Z

## Completed This Session (2026-03-29):

### MiniMax-M2 over-RAM inference bug fixes (3 commits, all on main)

- **decd668** `fix(inference): eliminate OOM during MoE graph build for over-RAM models`
  - `layers/core/ffn.go`: added `NewFFNFromDense` constructor
  - `inference/arch_deepseek.go`: use `NewFFNFromDense` in `buildExpertFFN` (eliminates ~857 GB phantom allocation)
  - `layers/core/linear.go`: fixed `NewLinearFromParam` missing `ops: engine.Ops()` (nil ops panic)

- **4ed3955** `fix(attention): apply QK norms before head reshape for MiniMax-M2`
  - `layers/attention/grouped_query_attention.go`: added `qkNormPreReshape` flag + `SetQKNormPreReshape`
  - `inference/arch_minimax_m2.go`: detect norm weight shape > headDim, set pre-reshape flag

- **0cd50bb** `docs: add verified inference result for MiniMax-M2 229B on 128 GB DGX Spark`
  - `README.md`: updated with load time + generation result
  - `docs/updates.md`, `docs/devlog.md`, `docs/benchmarks.md`: documented result

### Plan status
All waves 1-8 complete. No engineering tasks remain.

## In Progress:
- None

## Next Up (Backlog — no active engineering work):
- T4.7: Record video walkthrough (content task, ~4h)
- T5.4: Create Discord server (community ops, ~2h)
- ROCm backend (E8): requires AMD hardware (not available)
- Hardware benchmarks: T16.3 (needs A100/H100), T9.4 (multi-GPU), T29.4 (edge device)

## Known Blockers:
- GPU throughput for over-RAM inference: CUDA streaming GEMM not yet wired for mmap'd tensors
  on GPU side (CPU streaming GEMM is done). Would improve MiniMax-M2 from 0.06 to ~2-5 tok/s.
- Flaky tests: TestTSPulseInferAnomalyDetectSyntheticAnomaly, TestSchedulerImmediateEviction,
  TestGenerate_returnsResult — all pre-existing, pass in isolation.

## Merge Status:
- decd668: MERGED to main 2026-03-29
- 4ed3955: MERGED to main 2026-03-29
- 0cd50bb: MERGED to main 2026-03-29

## Ship Status:
- All three commits pushed directly to main.
- CI: passing (go build ./... clean, core tests pass)
- Staging: N/A
- Release: N/A (release-please generates release PR automatically)

## Deviations This Session:
- [Deviation: Bug] NewLinearFromParam missing ops field — nil dereference in WithSwiGLU. Fixed in decd668.
- [Deviation: Bug] MiniMax-M2 qNorm weight shape [6144] applied after reshape — shape mismatch. Fixed via qkNormPreReshape in 4ed3955.
- [Deviation: Bug] Four separate OOM sources during over-RAM inference. All four fixed across ztensor v0.14.1/v0.14.2 and zerfoo.
