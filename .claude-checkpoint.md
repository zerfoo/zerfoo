## Last Updated: 2026-03-14 00:05 UTC

## Completed This Session (Phase 5):

### Wave 1 (5 parallel teammates):
- T701.1: PGO profile collected (110KB, 13 merged samples)
- T702.1: Zero GC pauses during decode -- skip E702
- T703.1: 928 BCE, only 8 hot-path (<0.1%) -- skip E703
- T704.1: ~395 purego calls/token, ~20us (<0.4%) -- skip E704
- T705.1: LockOSThread added, caused 2.6% regression -- REVERTED

### Final Benchmark:
- PGO only (no LockOSThread): 189.95 tok/s (187.74, 190.75, 191.35)
- No measurable PGO improvement (GPU-bound workload)

**All 19 Phase 5 tasks complete.**

## Deviations:
- [Bug] LockOSThread regressed 190->185 tok/s. Reverted.
- E702 (GC), E703 (BCE), E704 (purego) skipped after audits showed negligible impact.
- PGO shows no improvement -- hot path is in CUDA kernels, not Go code.

## Final Result:
- **F32: ~190 tok/s** (stable across PGO/no-PGO)
- **Ollama: 197.21 tok/s**
- **Gap: 3.6%** (7 tok/s)
- The remaining gap is in CUDA kernel execution, not Go runtime overhead.

## Merge Status:
- All branches merged into main. Zero unmerged.

## Key Insight:
The 3% gap is NOT closable via Go-side optimizations. The Go runtime is not the
bottleneck -- GC has zero pauses, BCE is negligible, purego overhead is <0.4%,
and PGO has no effect because the hot path is CUDA kernel execution. To close
the remaining gap, the optimization must happen in CUDA kernels (Q4K GEMV) or
by enabling CUDA graph capture (requires making GQA position-independent).
