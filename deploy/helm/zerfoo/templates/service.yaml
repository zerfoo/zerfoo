apiVersion: v1
kind: Service
metadata:
  name: {{ include "zerfoo.fullname" . }}
  labels:
    {{- include "zerfoo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zerfoo.selectorLabels" . | nindent 4 }}
